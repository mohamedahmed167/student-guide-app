import 'package:flutter/material.dart';

class AcademicView extends StatelessWidget {
  const AcademicView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Academic screen")));
  }
}
