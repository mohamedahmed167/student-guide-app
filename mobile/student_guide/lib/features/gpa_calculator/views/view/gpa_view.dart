import 'package:flutter/material.dart';

class GPAView extends StatelessWidget {
  const GPAView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("GPA screen")));
  }
}
